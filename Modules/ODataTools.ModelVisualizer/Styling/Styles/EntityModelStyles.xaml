<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ODataTools.ModelVisualizer.Styling.Styles">

    <!--<Style x:Key="EntityKeyFieldStyle" TargetType="{x:Type Rectangle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=IsKey}" Value="True">
                <Setter Property="Height" Value="16" />
                <Setter Property="Width" Value="16" />
                <Setter Property="Fill" Value="{StaticResource AccentColorBrush}" />
                <Setter Property="OpacityMask">
                    <Setter.Value>
                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_key}" />
                    </Setter.Value>
                </Setter>
            </DataTrigger>            
        </Style.Triggers>
    </Style>-->

</ResourceDictionary>